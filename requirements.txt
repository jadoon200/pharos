# CPU-only torch wheels on Linux (CI/Docker); harmless no-op on macOS.
# The trajectory-anomaly model uses torch as its portable backend; we never need CUDA.
--extra-index-url https://download.pytorch.org/whl/cpu

sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
prefect>=3.0
structlog>=24.1
tenacity>=8.3
numpy>=1.26
scikit-learn>=1.5
fastapi>=0.115
uvicorn>=0.30
