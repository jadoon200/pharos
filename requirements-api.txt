# Slim runtime for the single-container deploy (read-only API + built dashboard, SQLite seed).
# CPU-only torch (the /score-track + /detect routes use the anomaly model); no Postgres driver,
# no Prefect, no scikit-learn (the PCA baseline is eval-only and imports lazily).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
structlog>=24.1
numpy>=1.26
torch>=2.2
